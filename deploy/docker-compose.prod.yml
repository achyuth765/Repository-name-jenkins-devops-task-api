services:
  task-api:
    image: task-api:${IMAGE_TAG}
    container_name: task-api-prod
    ports:
      - "3000:3000"
    restart: unless-stopped
    environment:
      NODE_ENV: production
    healthcheck:
      test:
        [
          "CMD",
          "node",
          "-e",
          "require('http').get('http://localhost:3000/health',r=>process.exit(r.statusCode===200?0:1)).on('error',()=>process.exit(1))"
        ]
      interval: 10s
      timeout: 5s
      retries: 5
